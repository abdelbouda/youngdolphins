---
const locale = Astro.currentLocale;
const alternativeLocale = locale === 'en' ? 'nl' : 'en';
const flag = alternativeLocale === 'en' ? '🇬🇧' : '🇳🇱';
---

<a 
  href={`/${alternativeLocale}`} 
  class="language-flag"
  aria-label={`Switch to ${alternativeLocale.toUpperCase()}`}
>
  {flag}
</a>
